# Code Generation

Lowering checked minipy modules to minivm bytecode, runtime representation, and
verification behavior.

## When to Read

Read this when changing the lowerer, runtime representation, generated bytecode,
optimization handoff, verification behavior, or host helper integration.

For checker rules that must reject invalid forms before lowering, read
`04-static-semantics.md`. For native symbol emitters, read `06-builtins.md`.

## Source of Truth

| Concern | Source |
|---|---|
| compile pipeline | `compiler/compiler.go` |
| checker metadata consumed by lowering | `compiler/check.go` |
| minivm builder usage | `compiler/` |
| native symbol emitters | `builtins/`, `operator/` |
| host ABI helpers | `hostabi/` |
| minivm verification | minivm `program.Verify` |

## Summary

The compiler lowers a checked `ast.Module` directly to minivm bytecode. It does
not emit Python bytecode or an intermediate Python object model.

`Compile` returns a verified `*program.Program` or a `token.ErrorList`/wrapped
error. The generated program is optimized at the configured minivm optimization
level and then verified with `program.Verify`.

## Entry Pipeline

`compiler.Compile(r, opts...)` performs:

1. read source bytes
2. parse with `parser.Parse`
3. create a module loader and load the entry module
4. check all reachable source modules and native symbols
5. lower modules and functions into a `program.Builder`
6. build a minivm program
7. optimize with `optimize.NewOptimizer(level)`
8. restore type and handler tables preserved across optimization
9. verify the final program

Compile options:

- `WithOutput(io.Writer)` sets the sink used by compiled `print`
- `WithOptimizationLevel(optimize.Level)` selects the minivm optimizer level
- `WithModules(fs.FS)` adds a module search root
- `WithModulePath(fs.FS, dirs...)` adds ordered search roots inside an `fs.FS`

## Modules and Imports

The loader resolves source modules from configured search roots and native modules
from a `module.Registry`. The default registry includes `builtins` and `operator`.

Imported source modules are checked before lowering. During lowering, imported
module bodies are emitted before their imported symbols are used. Native modules
are not emitted as source bytecode; their symbols lower through their registered
emit callbacks and host values.

Module/class/native-function objects are compile-time names, not first-class
runtime values.

## Runtime Representation

| Source construct | Lowering shape |
|---|---|
| `int` | minivm `i64` |
| `float` | minivm `f64` |
| `bool` | minivm boolean integer path |
| `str` | minivm string |
| `None`, `Any`, unions, iterators | reference values |
| `list[T]` | minivm array |
| `dict[K, V]` | minivm map |
| `set[T]` | minivm map from element to bool |
| `tuple[...]` | minivm struct |
| class instance | minivm struct with fixed fields |
| function | minivm function constant / global slot |
| captured local | boxed storage when shared with closures |

Unions and `Any` use dynamic reference paths and are narrowed statically where
possible. Specialized function clones can lower the same AST under concrete type
tables.

## Statements

### Assignments

Name assignments evaluate the RHS and store into the resolved local/global slot.
Annotated assignments without a value only declare/check the binding and emit no
runtime instruction.

Tuple/starred unpacking lowers by reading the source tuple/list fields or list
positions and storing each target. Starred rest targets build a list of the
remaining homogeneous elements.

Subscript assignment supports list and dict item writes. Slice assignment is not
lowered. Attribute assignment lowers to struct field writes.

`del` lowers to:

- zeroing a name slot
- `MAP_DELETE` for dict items
- native array delete/shift helper for list items
- zeroing a struct field for attributes

### Control Flow

`if`, `while`, and `for` lower with explicit labels and branches. The lowerer
mirrors checker static-truth pruning so impossible branches in specialized bodies
are not emitted.

`while` and `for` maintain loop-label stacks for `break` and `continue`. Loop
`else` blocks are emitted only along the non-break path.

`assert` lowers to a guard that constructs and throws a minivm error on failure.

### Match

`match` lowers to a linear decision tree. The subject is evaluated once into a
temporary slot. Each case emits a pattern test and optional guard, falls through to
the case body on match, and branches to the next case on mismatch.

### Try, Raise, With

`try` lowers to minivm try regions and explicit catch/finally paths. `raise`
constructs or forwards structured error values. `with` lowers through checked
context-manager entry/exit operations and inline finalizer frames.

`except*`, async forms, and scheduler/coroutine semantics are not lowered.

## Functions and Calls

Function definitions produce minivm function constants and store them in their
slots. Nested functions carry capture metadata; locals captured by inner functions
are boxed when needed.

Call lowering uses checker-produced call metadata:

- direct minipy calls lower to the resolved function or specialization
- default arguments are materialized at the call site by the checker metadata
- `*args` parameters receive synthetic list displays
- `**kwargs` parameters receive synthetic dict displays keyed by strings
- `*tuple` call expansion is compiled as tuple element loads
- unsupported dynamic `**expr` calls never reach lowering

### Specializations

For specializable functions, the checker records successful monomorphic
instantiations and their per-node type tables. Before emitting the entry module,
the compiler builds those specialized function constants. Direct calls that
resolved to a specialization call that concrete clone; other calls use the
original union/`Any` body.

Specialized bodies share the same AST but not the same expression type table,
which lets each clone lower branches, operators, and calls with concrete types.

## Expressions

### Operators

All operator syntax lowers through the `operator` package so syntax and
`operator.*` calls share one set of rules. Integer/float arithmetic uses minivm
numeric opcodes where possible; operations that need runtime helpers use native
host functions registered by the operator module.

### Containers and Strings

List, dict, set, and tuple displays lower to minivm array/map/struct creation
paths. String indexing/slicing and list iteration use host ABI helpers when the VM
primitive set does not provide the exact operation.

List methods lower without dynamic attribute lookup. `append`, `pop`, `insert`,
`extend`, and `reverse` mutate the receiver with minivm array opcodes and
compiler-emitted loops. `insert` grows with `ARRAY_APPEND` before shifting
elements right. `extend` records the source length before appending, preserving
`xs.extend(xs)` snapshot behavior. `index` uses a narrow host helper for equality
search and maps a miss to `ValueError`.

Contiguous list slice assignment and deletion lower through narrow host helpers.
The helpers normalize omitted and negative bounds like Python for step `1`,
clamp bounds into `[0, len(list)]`, replace only when `len(rhs)` equals the
normalized slice length, and store a resized array back into the receiver's heap
slot. Length mismatch maps to `ValueError`.

### Comprehensions and Generators

List/dict/set comprehensions lower as eager construction loops. Generator
expressions and generator functions lower to iterator/coroutine-style values and
are consumed by `next`/iteration paths.

### F-strings

F-strings lower to string-building operations. Replacement fields are converted
through printable string paths and checked conversions/format nesting before
lowering.

### Builtins and Native Calls

Builtins and `operator` functions are native module symbols. Each symbol provides
its own checker rule and emitter callback. Some builtin calls inline to minivm
opcodes (`len`, numeric conversions, truthiness); others call host functions
(`print`, string parsing, range/list iteration helpers, enumerate/zip helpers).

## Verification and Optimizer Notes

The compiler preserves the program type pool and exception handler table around
optimization, because the optimizer works on bytecode shape while those tables are
part of the verified program contract. Verification failures are wrapped as
`verify program: ...` errors.

## Related Docs

- `docs/README.md` — documentation map and ownership guide.
- `docs/spec/02-types.md` — source types and runtime mapping.
- `docs/spec/04-static-semantics.md` — checker guarantees consumed by lowering.
- `docs/spec/06-builtins.md` — native symbol checker and emitter behavior.
- minivm docs — bytecode, optimizer, and verifier behavior.
